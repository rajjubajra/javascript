//Main menu variable
const main = document.querySelector("#main");
const itemList = document.querySelectorAll('.item-list');
console.log(main);

main.addEventListener('mouseover', addShowId);

function addShowId (e){

   
}